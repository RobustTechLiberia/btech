# TODO: Banner Carousel Image Height & Gradient Overlay

## Steps

- [x] 1. Increase carousel wrapper height to `h-screen` in `banner.jsx`
- [x] 2. Remove `rounded-base` and intermediate heights (`h-56`, `md:h-96`) from wrapper
- [x] 3. Add `blue-900` gradient overlay (left-to-transparent) over the carousel
- [ ] 4. Verify carousel fills full screen with gradient overlay
